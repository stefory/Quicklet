<Window x:Class="Quicklet.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quicklet" 
        SizeToContent="Height" 
        Width="360"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Window.Resources>
        <!-- Brushes (Fallback for designer) -->
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#F5F5F7"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E5E5EA"/>
        <SolidColorBrush x:Key="SubBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextBrush" Color="#1C1C1E"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#8E8E93"/>
        <SolidColorBrush x:Key="PlaceholderBrush" Color="#C7C7CC"/>
        <SolidColorBrush x:Key="SelectedItemBackgroundBrush" Color="#E8F0FE"/>
        <SolidColorBrush x:Key="FooterBackgroundBrush" Color="#F5F5F7"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#007AFF"/>

        <!-- Button Style (Capsule) -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="4" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SelectedItemBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="4" 
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource WindowBackgroundBrush}" 
            CornerRadius="8" 
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.15" Direction="270" Color="Black"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 1. Header -->
            <Border Grid.Row="0" 
                    Background="Transparent"
                    Padding="16,12,16,8">
                <Grid>
                    <TextBlock x:Name="TitleTextBlock" Text="提示" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                </Grid>
            </Border>

            <!-- 2. Content -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,12,20,16">
                <!-- Info/Warning Icon -->
                <TextBlock x:Name="IconTextBlock" Text="💡" FontSize="20" Margin="0,0,12,0" VerticalAlignment="Top"/>
                <TextBlock x:Name="MessageTextBlock" 
                           Text="提示内容" 
                           FontSize="13" 
                           Foreground="{DynamicResource TextBrush}" 
                           TextWrapping="Wrap" 
                           VerticalAlignment="Center"
                           Width="240"/>
            </StackPanel>

            <!-- 3. Footer -->
            <Grid Grid.Row="2">
                <Border Height="1" VerticalAlignment="Top" Background="{DynamicResource BorderBrush}"/>
                <Border Background="{DynamicResource FooterBackgroundBrush}" 
                        Padding="16,10"
                        CornerRadius="0,0,8,8">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="确定" Click="Ok_Click" Width="80" Style="{StaticResource PrimaryButtonStyle}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
