<Window x:Class="Quicklet.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quicklet 设置中心" 
        Height="850" 
        Width="960"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Foreground="{DynamicResource TextBrush}"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown"
        PreviewMouseDown="Window_PreviewMouseDown"
        FontFamily="Segoe UI, Microsoft YaHei, sans-serif">

    <Window.Resources>
        <!-- Brushes (Fallback for designer) -->
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#F5F5F7"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E5E5EA"/>
        <SolidColorBrush x:Key="SubBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextBrush" Color="#1C1C1E"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#8E8E93"/>
        <SolidColorBrush x:Key="PlaceholderBrush" Color="#C7C7CC"/>
        <SolidColorBrush x:Key="SelectedItemBackgroundBrush" Color="#E8F0FE"/>
        <SolidColorBrush x:Key="FooterBackgroundBrush" Color="#F5F5F7"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#007AFF"/>
        <!-- Modern Slim ScrollBar Style (Windows 11 / Minimalist) -->
        <Style TargetType="Thumb" x:Key="ScrollBarThumbStyle">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="MinHeight" Value="24"/>
            <Setter Property="MinWidth" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="border"
                                CornerRadius="4"
                                Background="{DynamicResource MutedTextBrush}"
                                Opacity="0.4"
                                Margin="1,0">
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="1.0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RepeatButton" x:Key="ScrollBarPageButtonStyle">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{TemplateBinding Background}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="VerticalScrollBarTemplate" TargetType="ScrollBar">
            <Grid x:Name="Bg" Background="Transparent" SnapsToDevicePixels="True">
                <Track x:Name="PART_Track" IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}" Command="{x:Static ScrollBar.PageUpCommand}"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollBarThumbStyle}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}" Command="{x:Static ScrollBar.PageDownCommand}"/>
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <ControlTemplate x:Key="HorizontalScrollBarTemplate" TargetType="ScrollBar">
            <Grid x:Name="Bg" Background="Transparent" SnapsToDevicePixels="True">
                <Track x:Name="PART_Track" IsDirectionReversed="False">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}" Command="{x:Static ScrollBar.PageLeftCommand}"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollBarThumbStyle}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}" Command="{x:Static ScrollBar.PageRightCommand}"/>
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>


        <Style TargetType="ScrollBar">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template" Value="{StaticResource VerticalScrollBarTemplate}"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="8"/>
                    <Setter Property="Template" Value="{StaticResource HorizontalScrollBarTemplate}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Button Style (Capsule) -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SubBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="6" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SelectedItemBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="6" 
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Material 3 TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                CornerRadius="6" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ToggleSwitch Style (Windows style) -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="40" Height="20" VerticalAlignment="Center" Margin="0,0,8,0">
                                <Border x:Name="BackgroundBorder" 
                                        CornerRadius="10" 
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"/>
                                <Ellipse x:Name="Thumb" 
                                         Width="12" Height="12" 
                                         Fill="{DynamicResource MutedTextBrush}" 
                                         HorizontalAlignment="Left"
                                         Margin="4,0,0,0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTranslate" X="0"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="BackgroundBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="#FFFFFF"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb" 
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" 
                                                             To="20" 
                                                             Duration="0:0:0.12"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb" 
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" 
                                                             To="0" 
                                                             Duration="0:0:0.12"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Segmented Control RadioButton Style -->
        <Style x:Key="SegmentedRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                Margin="2">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{DynamicResource SegmentedSelectedBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.1" Color="Black"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="true"/>
                                    <Condition Property="IsChecked" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="border" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="Opacity" TargetName="border" Value="0.5"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Styles -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="Transparent"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
            <Setter Property="Padding" Value="12,8,12,12"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="Transparent" 
                                BorderThickness="0">
                            <ContentPresenter Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SelectedItemBackgroundBrush}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SelectedItemBackgroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Outer Container -->
    <Border Margin="30"
            Background="{DynamicResource WindowBackgroundBrush}" 
            CornerRadius="12" 
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="12" Opacity="0.3" Direction="270" Color="Black"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Title Bar -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- 1. Title Bar (Borderless & Branded) -->
            <Border Grid.Row="0" 
                    Background="Transparent" 
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="34">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="16,10,0,0">
                        <!-- Brand Icon (Lightning) -->
                        <Path Data="M7,2 L17,2 L13,10 L19,10 L9,22 L11,13 L5,13 Z" 
                              Fill="{DynamicResource AccentBrush}" 
                              Width="12" Height="14" 
                              Stretch="Uniform" 
                              VerticalAlignment="Center" 
                              Margin="0,0,8,0"/>
                        
                        <!-- Branded Title -->
                        <TextBlock Text="Quicklet" FontWeight="Bold" FontSize="13" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                        
                        <!-- Separator Dot (Crisp Vector Ellipse) -->
                        <Ellipse Width="4" Height="4" Fill="{DynamicResource MutedTextBrush}" VerticalAlignment="Center" Margin="8,1,8,0" Opacity="0.75"/>
                        
                        <!-- Subtitle -->
                        <TextBlock Text="设置中心" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Close Button -->
                    <Button HorizontalAlignment="Right" VerticalAlignment="Top" Click="Cancel_Click"
                            Width="46" Height="32" BorderThickness="0" Background="Transparent">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="CloseBorder" Background="Transparent" CornerRadius="0,11,0,0">
                                    <Path x:Name="ClosePath" Data="M0,0 L10,10 M10,0 L0,10" 
                                          Stroke="{DynamicResource TextBrush}" 
                                          StrokeThickness="1" 
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center" 
                                          Width="10" Height="10" 
                                          Stretch="Fill"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="CloseBorder" Property="Background" Value="#E81123"/>
                                        <Setter TargetName="ClosePath" Property="Stroke" Value="#FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- 2. Content Area -->
            <Grid Grid.Row="1" Margin="20,4,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- General Settings Card -->
                <Border Grid.Row="0"
                        Background="{DynamicResource SubBackgroundBrush}" 
                        CornerRadius="12" 
                        BorderBrush="{DynamicResource BorderBrush}" 
                        BorderThickness="1" 
                        Padding="16,12" 
                        Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="通用" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,16"/>
                            
                            <!-- Global Hotkey -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="全局快捷键" FontSize="13" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
                                    <StackPanel x:Name="HotkeyKeycapsPanel" MouseLeftButtonDown="HotkeyKeycapsPanel_MouseLeftButtonDown" Orientation="Horizontal" Cursor="Hand" Background="Transparent" ToolTip="点击修改快捷键">
                                        <!-- Keycaps will be generated dynamically in code-behind or shown visually -->
                                        <Border Background="{DynamicResource BorderBrush}" CornerRadius="4" Padding="8,4" Margin="4,0">
                                            <TextBlock Text="Alt" FontSize="12" Foreground="{DynamicResource TextBrush}"/>
                                        </Border>
                                        <Border Background="{DynamicResource BorderBrush}" CornerRadius="4" Padding="8,4" Margin="4,0">
                                            <TextBlock Text="Space" FontSize="12" Foreground="{DynamicResource TextBrush}"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBox x:Name="HotkeyTextBox" 
                                             Width="0" Height="0"
                                             Visibility="Collapsed"/>
                                </StackPanel>
                            </Grid>
                            
                            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,0,0,10"/>

                            <!-- Theme -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="界面主题" FontSize="13" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                <Border Grid.Column="1" HorizontalAlignment="Right"
                                        Background="{DynamicResource WindowBackgroundBrush}" 
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8" 
                                        Padding="2">
                                    <StackPanel Orientation="Horizontal">
                                        <RadioButton x:Name="RadioLightTheme" Content="浅色" GroupName="Theme" Style="{StaticResource SegmentedRadioButtonStyle}" Checked="Theme_Checked"/>
                                        <RadioButton x:Name="RadioDarkTheme" Content="深色" GroupName="Theme" Style="{StaticResource SegmentedRadioButtonStyle}" Checked="Theme_Checked"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,0,0,10"/>

                            <!-- Search Engine -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="默认搜索引擎" FontSize="13" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                <Border Grid.Column="1" HorizontalAlignment="Right"
                                        Background="{DynamicResource WindowBackgroundBrush}" 
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8" 
                                        Padding="2">
                                    <StackPanel Orientation="Horizontal">
                                        <RadioButton x:Name="RadioGoogle" Content="Google" GroupName="SearchEngine" Style="{StaticResource SegmentedRadioButtonStyle}" Checked="SearchEngine_Checked"/>
                                        <RadioButton x:Name="RadioBaidu" Content="百度" GroupName="SearchEngine" Style="{StaticResource SegmentedRadioButtonStyle}" Checked="SearchEngine_Checked"/>
                                        <RadioButton x:Name="RadioBing" Content="必应" GroupName="SearchEngine" Style="{StaticResource SegmentedRadioButtonStyle}" Checked="SearchEngine_Checked"/>
                                        <RadioButton x:Name="RadioCustom" Content="自定义" GroupName="SearchEngine" Style="{StaticResource SegmentedRadioButtonStyle}" Checked="SearchEngine_Checked"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Custom URL (Dynamically Shown) -->
                            <Grid x:Name="CustomUrlContainer" Margin="0,0,0,10" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="CustomUrlLabel" Grid.Column="0" Text="自定义 URL 用 {query} 占位" FontSize="13" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                <TextBox x:Name="DefaultSearchTextBox" Grid.Column="1" HorizontalAlignment="Right" Width="260"/>
                            </Grid>

                            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,0,0,10"/>

                            <!-- Startup -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="开机启动" FontSize="13" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                <CheckBox x:Name="StartupCheckBox" 
                                          Grid.Column="1" 
                                          HorizontalAlignment="Right"
                                          Style="{StaticResource ToggleSwitchStyle}"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                <!-- Rules Table Card -->
                <Border Grid.Row="1"
                        Background="{DynamicResource SubBackgroundBrush}" 
                        CornerRadius="12" 
                        BorderBrush="{DynamicResource BorderBrush}" 
                        BorderThickness="1" 
                        Padding="20" 
                        Margin="0,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="关键字与网址映射" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,16"/>

                        <DataGrid Grid.Row="1" 
                                  x:Name="RulesDataGrid" 
                                  EnableRowVirtualization="True"
                                  VirtualizingStackPanel.IsVirtualizing="True"
                                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="0,0,6,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="触发词" Binding="{Binding Keyword}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#007AFF"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="显示名称" Binding="{Binding Name}" Width="140"/>
                                <DataGridTextColumn Header="直达 URL" Binding="{Binding Url}" Width="*"/>
                                <DataGridTextColumn Header="带参搜索 URL" Binding="{Binding SearchUrl}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Grid Grid.Row="2" Margin="0,16,0,0">
                            <Button Content="+ 添加规则" 
                                    Click="AddRow_Click"
                                    Background="Transparent"
                                    Foreground="#007AFF"
                                    BorderThickness="0"
                                    FontWeight="SemiBold"
                                    Padding="0"
                                    HorizontalAlignment="Left"/>
                            <Button Content="删除选中" 
                                    Click="DeleteRow_Click"
                                    Background="{DynamicResource WindowBackgroundBrush}"
                                    Foreground="{DynamicResource TextBrush}"
                                    BorderThickness="0"
                                    HorizontalAlignment="Right"/>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <!-- 3. Footer -->
            <Grid Grid.Row="2">
                <Border Height="1" VerticalAlignment="Top" Background="{DynamicResource BorderBrush}"/>
                <Border Background="{DynamicResource FooterBackgroundBrush}" 
                        Padding="20,16"
                        CornerRadius="0,0,12,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="💡" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="双击表格单元格可直接编辑;带参搜索 URL 留空则仅支持网页直达。" 
                                       Foreground="{DynamicResource MutedTextBrush}" 
                                       FontSize="12" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="取消" 
                                    Click="Cancel_Click"
                                    Margin="0,0,12,0"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="{DynamicResource TextBrush}"/>
                            <Button Content="保存并应用" 
                                    Click="Save_Click"
                                    Style="{StaticResource PrimaryButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
